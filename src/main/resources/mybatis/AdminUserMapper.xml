<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.llama.hub.mapper.AdminUserMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin_user (username, password_hash, created_at, updated_at)
        VALUES (#{username}, #{passwordHash}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findByUsername" resultType="com.llama.hub.model.AdminUser">
        SELECT id, username, password_hash, created_at, updated_at
        FROM admin_user
        WHERE username = #{username}
    </select>

    <update id="update">
        UPDATE admin_user
        SET password_hash = #{passwordHash},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM admin_user
    </select>

</mapper>
